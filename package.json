{
  "name": "storyofyourlife",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-redux": "^5.0.7",
    "react-scripts": "1.1.5",
    "redux": "^4.0.0",
    "redux-thunk": "^2.3.0"
  },
  "scripts": {
    "start": "flow && react-scripts start",
    "build": "flow && react-scripts build",
    "test": "flow && react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "flow": "flow",
    "postinstall": "flow-typed install"
  },
  "devDependencies": {
    "flow-bin": "^0.80.0",
    "flow-typed": "^2.5.1"
  }
}
